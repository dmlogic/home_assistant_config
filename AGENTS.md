# Home Assistant config

This is a local backup of a Home Assistant config.

When running on the owner's network you may access directly via `ssh hass` to perform diagnostics.

You may also perform configuration changes if explicitly authorised and if a recent backup is verified.
